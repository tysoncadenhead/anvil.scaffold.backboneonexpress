<table class="table table-striped">
<% _.each({{name}}s, function ({{name}}) { %>
	<tr>
		<td><a href="/{{name}}/<%= {{name}}._id %>"><%= {{name}}._id %><a></td>
	</tr>
<% }) %>
</table>
<a href="/{{name}}/new" class="btn">New {{name}}</a>